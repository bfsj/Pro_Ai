# 实验报告：KV Cache 对单卡推理的影响

状态：待在 GPU 上实际运行和填写。本文中的空项不是实验结论。

## 问题与假设

我想验证的问题：

运行前的假设，以及可能推翻它的观测：

## 可复现配置

- GPU / 显存 / 驱动 / 功耗限制：
- 系统 / Python / PyTorch / CUDA runtime / Transformers：
- 仓库 commit / 模型及 revision / 精度 / Attention backend：
- 输入长度 / batch / 输出长度 / seed / 预热 / 重复次数：
- 测速命令：
- 其他 GPU 进程及机器负载：
- 原始结果与图表路径：

## 正确性

- FP32、BF16 的最大绝对误差与阈值：
- 检查了哪些 batch 和输入长度：
- 理论与实际 KV 字节数：

## 测量结果

| 输入长度 | Batch | 缓存 | Prefill+首 token (ms) | Decode/步 (ms) | 总输出吞吐 (tokens/s) | 峰值 allocated (MiB) |
| --- | --- | --- | --- | --- | --- | --- |
| 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |

## 原因分析

1. 缓存收益随输入长度怎样变化？
2. batch 对吞吐和每步延迟的影响是否一致？
3. 哪些现象有 trace 或算子时间支持，哪些仍是假设？
4. 有无 OOM、异常波动或不符合预期的结果？

## 局限和下一次实验

当前是等长、随机 token、固定步数的离线执行，没有网络、在线排队或真实业务数据分布。说明结论能覆盖什么场景，下一次只改变哪个因素。

## 可用于简历的真实贡献

分别记录：采用的参考代码；自己理解并修改的部分；完成的实验与发现；尚未完成的工作。只写有原始记录支持的数字，不将框架已有优化写成个人实现。
